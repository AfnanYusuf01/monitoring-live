<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= success ? 'Verifikasi Berhasil' : 'Verifikasi Gagal' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @keyframes scaleIn {
            from { transform: scale(0); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
        @keyframes drawCheck {
            from { stroke-dashoffset: 50; }
            to { stroke-dashoffset: 0; }
        }
        .animate-scale-in {
            animation: scaleIn 0.5s ease-out forwards;
        }
        .animate-draw-check {
            animation: drawCheck 0.8s ease-in-out forwards;
            stroke-dasharray: 50;
            stroke-dashoffset: 50;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl shadow-xl p-6 md:p-8 max-w-md w-full mx-auto">
        <% if (success) { %>
            <!-- Success Animation -->
            <div class="flex justify-center mb-6">
                <div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center relative">
                    <div class="w-20 h-20 bg-green-200 rounded-full flex items-center justify-center animate-scale-in">
                        <svg class="w-12 h-12 text-green-600" viewBox="0 0 24 24" fill="none">
                            <path d="M7 13L10 16L17 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="animate-draw-check" />
                        </svg>
                    </div>
                </div>
            </div>

            <!-- Content -->
            <div class="text-center">
                <h1 class="text-2xl font-bold text-gray-800 mb-2">Verifikasi Berhasil!</h1>
                <p class="text-gray-600 mb-4">Pembayaran User telah berhasil diverifikasi</p>
                
                <!-- Order Details -->
                <div class="bg-gray-50 rounded-lg p-4 mb-4 text-left">
                    <h3 class="font-semibold text-gray-700 mb-3">Detail Transaksi</h3>
                    
                    <div class="space-y-2 text-sm">
                        <div class="flex justify-between">
                            <span class="text-gray-600">No. Transaksi:</span>
                            <span class="font-medium"><%= data.transactionId %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Nama Pelanggan:</span>
                            <span class="font-medium"><%= data.user.name %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Email:</span>
                            <span class="font-medium"><%= data.user.email %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Paket Langganan:</span>
                            <span class="font-medium"><%= data.subscription.name %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Durasi:</span>
                            <span class="font-medium"><%= data.subscription.duration %> hari</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Jumlah Pembayaran:</span>
                            <span class="font-medium">Rp <%= data.amount.toLocaleString('id-ID') %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Status:</span>
                            <span class="font-medium text-green-600">Berhasil</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Mulai Berlaku:</span>
                            <span class="font-medium"><%= data.startDate %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Berlaku Hingga:</span>
                            <span class="font-medium"><%= data.endDate %></span>
                        </div>
                    </div>
                </div>
                
                <!-- Email notification message -->
                <div class="bg-blue-50 rounded-lg p-4 mb-4 text-left">
                    <div class="flex items-start">
                        <div class="flex-shrink-0">
                            <i class="fas fa-envelope text-blue-500 mt-1"></i>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm text-blue-700">
                                Notifikasi konfirmasi telah terkirim ke email Anda. Silakan cek inbox atau folder spam Anda.
                            </p>
                        </div>
                    </div>
                </div>
                
                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row gap-3 mt-8">
                    <a href="/" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition flex-1 flex items-center justify-center gap-2">
                        <i class="fas fa-home"></i> Kembali ke Beranda
                    </a>
                    <a href="/dashboard" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition flex-1 flex items-center justify-center gap-2">
                        <i class="fas fa-tachometer-alt"></i> Ke Dashboard
                    </a>
                </div>
            </div>
        <% } else { %>
            <!-- Error State -->
            <div class="flex justify-center mb-6">
                <div class="w-24 h-24 bg-red-100 rounded-full flex items-center justify-center relative">
                    <div class="w-20 h-20 bg-red-200 rounded-full flex items-center justify-center animate-scale-in">
                        <i class="fas fa-exclamation-triangle text-red-600 text-3xl"></i>
                    </div>
                </div>
            </div>

            <div class="text-center">
                <h1 class="text-2xl font-bold text-gray-800 mb-2">Verifikasi Gagal</h1>
                <p class="text-gray-600 mb-6"><%= message %></p>
                
                <div class="flex flex-col sm:flex-row gap-3 mt-8">
                    <a href="/" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition flex-1 flex items-center justify-center gap-2">
                        <i class="fas fa-home"></i> Kembali ke Beranda
                    </a>
                    <a href="/contact" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition flex-1 flex items-center justify-center gap-2">
                        <i class="fas fa-headset"></i> Hubungi Support
                    </a>
                </div>
            </div>
        <% } %>
    </div>

    <script>
        // Menambahkan delay untuk animasi centang
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(function() {
                const checkmark = document.querySelector('.animate-draw-check');
                if (checkmark) {
                    checkmark.style.animation = 'drawCheck 0.8s ease-in-out forwards';
                }
            }, 300);
        });
    </script>
</body>
</html>